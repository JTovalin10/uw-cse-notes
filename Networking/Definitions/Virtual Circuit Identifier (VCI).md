## Virtual Circuit Identifier (VCI)
A small label in the packet header that identifies which [[Virtual Circuit (VC)|VC]] a packet belongs to at a given switch. Has **link-local scope** — only needs to be unique per port per switch, not globally. Gets swapped at each hop.
