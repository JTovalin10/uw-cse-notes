# CSE391: Tentatively Typed

**[[Tentatively Typed]]** refers to the nature of Bash where variables do not have strict data types (like `int` or `boolean`). Instead, most variables are treated as strings unless used in an arithmetic context.
