The concept is the same as makefiles in C [[Make Files (c)]]
the only difference is the gcc command, c uses:
```c
gcc -Wall -g -std=c17 -o/c main.o/c
```

In c++ the command is
```c++
g++ -Wall -g -std=c++17 -o/cc main.o/cc
```
