Any function with the virtual keyword is on the V-table and any function that overrides a virtual function also goes onto the V-table. This will chain so a override of an override is on the virtual table

Each class has its own virtual table

[[Upcasting]]