# CSE341: Binding

An association between a name (identifier) and a value or a function definition. Bindings can be local (created by `let`) or global (created by `define` at the top level).

See: [[CSE341/Trefoil Basics/Trefoil Functions and Scoping|Trefoil Functions and Scoping]]
