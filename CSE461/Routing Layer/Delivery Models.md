unicast: one single source, one destination

broadcast: one src and multiple destinations
- sports game
- streaming

multicast: single transmitter and transmit to subset nodes in the network

Anycast: any subset of nodes transmitting to any subset of nodes in the network