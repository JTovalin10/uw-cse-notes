**Transmission Control Protocol.** A transport-layer protocol that provides a **reliable, ordered byte-stream** channel between two endpoints. Handles retransmission, flow control, and congestion control.

One of the two main transport protocols in the Internet architecture, alongside [[UDP]].
