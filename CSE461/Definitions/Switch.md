A device that interconnects links to form a larger network. Multi-input/output — transfers packets from an input to one or more outputs.

- Fixed number of I/O ports
- Connected to each other and to hosts using point-to-point links
- Adding new hosts doesn't necessarily reduce performance for existing hosts
- More scalable than shared-media networks — can support multiple hosts at full speed
