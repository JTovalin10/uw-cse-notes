A type of [[Source Routing]] where only a set of waypoints is specified — how to get between them is left to the network.

- Reduces the topology knowledge the source needs
- Helpful in large networks where getting complete path info for a [[Strict Source Route]] is difficult
