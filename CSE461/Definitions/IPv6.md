# IPv6

**[[IPv6]]** (Internet Protocol version 6) is the successor to IPv4, designed to solve address exhaustion by using **128-bit** addresses. It simplifies the core packet header to improve routing performance, removes redundant features like the header checksum, and natively supports modern networking needs like auto-configuration and improved security.
