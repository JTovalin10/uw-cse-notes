# Encapsulation (stack)

**[[CSE461/Definitions/Encapsulation (stack)|Encapsulation (stack)]]**: The process of wrapping data from one protocol layer into the payload of a lower layer as it moves down the protocol stack.