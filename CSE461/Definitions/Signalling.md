The process where a host sends a control message into the network to dynamically establish a [[Virtual Circuit]]. The message propagates switch-by-switch, creating connection state along the path.

Results in a [[Switched Virtual Circuit]]. Contrast with [[Permanent Virtual Circuit]] which is manually configured.
