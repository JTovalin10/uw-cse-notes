![[Screenshot 2026-02-09 at 12.47.28 PM.png]]
the quality of service is the reason why virtual circuits is still around
1. virtual circuits id can be used as a direct index instead of an indirect key to [[Forwarding Table]]
2. virtual circuitrs can have lower per-packet overhead
3. routing only needs to happen once per-flow instead of per-packet