## In Time
can find the width of a bit with:
$$
\text{W(t)} = \frac{1}{\text{bandwidth}}
$$
## On Physical Wire
To find this we solve for width of bit in time and multiply it by the propagation delay
$$
\text{W(p) = W(t) * Pd}
$$
