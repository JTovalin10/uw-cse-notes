Nodes that interconnect networks in an [[Internetwork (IP)|internetwork]]. Also called **gateways**.

Each router is connected to at least two physical networks and forwards packets between them using a [[Forwarding Table]].
