# Routing Among Mobile Devices

**Problem:** Host moves → new IP. Correspondent still sends to old address.

**Solution:** [[Mobile IP]] — [[Home Address (Mobile IP)|home address]] (stable) + [[Care-of Address (Mobile IP)|care-of address]] (current). [[Home Agent (Mobile IP)|Home agent]] intercepts, tunnels to care-of.

## Notes

- [[Mobile Networking Challenges]] — The problem, identifier vs locator
- [[Mobile IP]] — [[Home Address (Mobile IP)|Home address]], [[Care-of Address (Mobile IP)|care-of]], [[Home Agent (Mobile IP)|home agent]], [[Foreign Agent (Mobile IP)|foreign agent]]
- [[Mobile IP Route Optimization]] — Binding cache, triangle routing
- [[Mobility in IPv6]] — IPv6 differences, extension headers
