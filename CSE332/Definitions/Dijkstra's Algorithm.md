# Dijkstra's Algorithm

**[[Dijkstra's Algorithm]]** is a graph search algorithm that finds the single-source shortest path in a weighted graph with non-negative edge weights. It maintains a set of "visited" nodes and uses a priority queue to greedily explore the closest unvisited nodes, updating the shortest-known distance to each neighbor as it progresses.
