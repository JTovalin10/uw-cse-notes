**Unrepeatable Read**: A situation where a transaction reads the same data item twice and finds different values because another transaction modified and committed that data in between the two reads.
