- clients can provide 0 or more request headers

**Host**: the DNS name of the server
**User-Agent**: identifying string naming the browser
**Accept**: the content type the client prefers or can accept
**Cookie**: an HTTP cookie prev set by the server
