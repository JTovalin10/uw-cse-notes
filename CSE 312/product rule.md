if you have a sequential process and want to find the number of possibilities, where step 1 has $n_1$ options,..., step k has $n_k$ options then the formula is $$n_1 \cdot n_2 \cdot ... \cdot n_k$$
If we are 
