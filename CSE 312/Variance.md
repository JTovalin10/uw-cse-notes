The variance of a random variable X is (standard deviation)
$$
Var(x) = \sum_w P(w)(X(w) - E[X])^2 = E[X^2] - (E[X])^2
$$
Furthermore, if X and Y are indepedent then 
$$
Var(X + Y) = Var(X) + Var(Y)
$$
Some facts about Variance:
- Var(X + c) = Var(X)
- Var(aX) = $a^2$ Var(X)
[[Random Variables]]