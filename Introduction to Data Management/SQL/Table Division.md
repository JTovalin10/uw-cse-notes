# SQL: Table Division

Given relations R(A, B) and S(B), their division is:
$$
R \div S = \{ a \mid \forall b \in S \Rightarrow (a, b) \in R\}
$$

It is called division because it is the largest T with the property:
$$
T \times S \subseteq R
$$
