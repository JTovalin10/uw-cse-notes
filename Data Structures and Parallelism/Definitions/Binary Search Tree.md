# Binary Search Tree

A **[[Binary Search Tree]] (BST)** is a binary tree where each node maintains the **Order Property**: all values in the left subtree are strictly smaller than the node's value, and all values in the right subtree are strictly larger. This structure allows for efficient searching, as each comparison eliminates approximately half of the remaining tree (if balanced).
