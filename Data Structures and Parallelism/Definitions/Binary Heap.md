# Binary Heap

A **[[Binary Heap]]** is a priority queue data structure implemented as a complete binary tree that satisfies the **Heap Property**: the value of each node is greater than or equal to (max-heap) or less than or equal to (min-heap) the values of its children. It achieves $\Theta(\log n)$ worst-case time for both insertion and extraction of the extreme element.
