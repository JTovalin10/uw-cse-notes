# Dynamic Growth
```
Heap needs more space?
→ Allocate new page, add entry to page table
→ No need to move existing pages
```

Unlike [[Base and Bounds]] where processes were stuck with fixed allocations, virtual memory allows the heap and stack to grow dynamically.
