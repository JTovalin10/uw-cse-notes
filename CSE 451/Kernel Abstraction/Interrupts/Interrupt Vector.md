array of function pointer that tells the CPU where to jump when a interrupt happens

this creates safety as we have controlled entry points so the user cannot just jump where ever they want
![[Screenshot 2026-01-07 at 12.00.18 PM.png]]

# Security
Contains a checksum as before someone can make it point to their code