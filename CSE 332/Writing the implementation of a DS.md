- high level description of the operations, include any potential errors with **raise**
- mention the types of data structures it can use like linkedlist or array and define what the linkedlist will do for each one
- then say why its useful
ex
stack
pop: removes the last item, raises an error if its empty