# CSE341: Dynamic Environment

### Formal Definition

A mapping from variable names to their fully evaluated runtime values, maintained during the evaluation phase of a program.

### Simplified Explanation

The computer's memory during program execution that remembers the actual computed values (like `5` or `"hello"`) assigned to variables.

See: [[Syntax and Semantics|Syntax and Semantics]]
