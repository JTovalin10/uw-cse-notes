1-to-1 mapping where each memory address maps to exactly one specific cache line. Since the target line is determined solely by the index bits, only that one line's tag needs to be compared — no other slots are checked. This is simple and fast but prone to conflict misses, since two addresses that share the same index will constantly evict each other even if other cache lines are free.
