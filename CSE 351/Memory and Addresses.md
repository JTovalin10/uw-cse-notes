# Words
Word size is the number of bits used by the CPU in execution of instructions (8 bytes = 64-bits)
# Memory
(more specifically random-access memory, or RAM) is a separate piece of hardware that acts as a local pool of data storage for the CPU during the execution of instructions.
# # Addresses
each byte is given a unique memory address represented in byte. it is fixed by the ram you have
[[Base Representation]]