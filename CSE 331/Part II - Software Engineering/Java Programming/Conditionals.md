[[Floyd Logic]]
Inside of floyd logic
```c
if (a >= 0 && b >= 0) {
 {{ a >= 0 and b >= 0 }}
}
```
# if else
- Reason separately about each path to a return
	- handle each path the same as before
	- but now there can be multiple paths to one return